"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export default function RegisterClient() {
  const router = useRouter();
  // ... form kodların, handle submit fonksiyonların vb.
  return <form>{/* Form içeriğin */}</form>;
}
